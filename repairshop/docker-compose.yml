services:
  repairshop:
    build:
      context: .
      dockerfile: Dockerfile
    image: repairshop:latest
    container_name: repairshop
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env.production
